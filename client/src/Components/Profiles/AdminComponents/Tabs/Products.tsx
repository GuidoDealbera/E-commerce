// import { useSelector } from "react-redux"
// import { RootState } from "../../../../Store/store"

const Products = () => {
  // const {user} = useSelector((state: RootState) => state.profile);
  // const products = user?.Products;
  //aqui se va a mapear los productos que haya publicado el admin y se mostrarán
  //cards de cada producto, sin la posibilidad de que pueda comprarse a si mismo el admin...
  return (
    <div>Productos</div>
  )
}

export default Products